export interface SearchFilterForm {
  city: string;
  startDate: string;
  finishDate: string;
  peopleNum:number;
// }
// export interface FilterForm{
  amenities: number[];
  minPrice:number;
  maxPrice:number;
  accommodationType:string;
}
